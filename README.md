# Lead Distribution CRM

Мини-CRM система для распределения лидов между операторами по различным источникам (ботам).

## 🚀 Быстрый запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск сервера
uvicorn main:app --reload --host 0.0.0.0 --port 8000 
```
После запуска откройте: http://localhost:8000/docs для доступа к Swagger UI.


## 🔧 API Эндпоинты
#### Управление операторами

    POST /operators/ - создание оператора

    GET /operators/ - список всех операторов с текущей нагрузкой

#### Управление источниками

    POST /sources/ - создание источника

    POST /sources/{source_id}/assign-operators/ - назначение операторов на источник с весами

#### Работа с обращениями

    POST /tickets/ - создание нового обращения (основной эндпоинт)

    GET /tickets/ - список всех обращений

    GET /leads/ - список всех лидов